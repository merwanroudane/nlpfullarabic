# ---- الأساس: البيانات والحساب — core data and computation ----
numpy>=1.26.0
pandas>=2.2.0
scipy>=1.14.0
python-dateutil>=2.8.0

# ---- سحب البيانات من الويب — web scraping ----
requests>=2.32.0
bs4>=0.0.2
lxml>=5.0.0
tqdm>=4.67.0
# لازم لاستخراج نصّ النصوص الحرفية للاجتماعات والمؤتمرات الصحفية من ملفّات pdf.
# بدونه تفشل حزمة fomc_get_data في الاستيراد كلّها، لا وحدات pdf وحدها.
# Required to extract text from the meeting and press-conference transcript PDFs.
# Without it the whole fomc_get_data package fails to import, not just the PDF modules.
textract>=2.0.0

# ---- الرسم — plotting ----
matplotlib>=3.9.0
seaborn>=0.13.0
wordcloud>=1.9.0
scikit-plot>=0.3.7

# ---- معالجة اللغة الطبيعية — natural language processing ----
nltk>=3.9.0
torch>=2.6.0
transformers>=4.40.0

# ---- تعلّم الآلة — machine learning ----
scikit-learn>=1.6.0
xgboost>=2.0.0
lightgbm>=4.0.0
catboost>=1.2.0

# ---- التفسير والقياس الاقتصادي — interpretation and econometrics ----
shap>=0.44.0
statsmodels>=0.14.0

# ---- اختياري — optional ----
# imbalanced-learn>=0.12.0   # لتقنية SMOTE؛ البديل الافتراضي هو أوزان الفئات
#                              # for SMOTE; the default alternative is class weights
